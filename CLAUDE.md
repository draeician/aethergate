# Claude Code Configuration
- Read `AETHERGATE_RULES.md` for architectural constraints.
- Use `uvicorn` for running the server.
- Use `pytest` for testing.
- When asked to "scaffold", create the directory structure using `mkdir -p` and `touch`.
